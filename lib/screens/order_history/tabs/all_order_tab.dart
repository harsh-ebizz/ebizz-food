import 'package:flutter/material.dart';

class AllOrderTab extends StatelessWidget {
  const AllOrderTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("fights");
  }
}
