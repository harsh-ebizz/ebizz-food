import 'package:flutter/material.dart';

class CanceledTab extends StatelessWidget {
  const CanceledTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("fights");
  }
}
